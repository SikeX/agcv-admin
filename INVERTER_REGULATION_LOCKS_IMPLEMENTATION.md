# 逆变器调节闭锁功能实现文档

## 功能概述

本次实现了逆变器上下调节闭锁功能，用于AGC（自动发电控制）系统中，根据所有逆变器的调节能力自动设置调节闭锁状态。

### 业务需求

1. **上调节闭锁**：当所有逆变器都不能再往上调节时，自动设置上调节闭锁
2. **下调节闭锁**：当所有逆变器都不能再往下调节时，自动设置下调节闭锁

## 实现方案

### 1. 核心逻辑

在AGC控制周期（`executeAGCCycle`）的开始阶段，增加了闭锁状态检查和更新逻辑：

```
AGC控制周期执行流程：
1. 获取并网点配置
2. 检查并更新调节闭锁状态 ✅ 新增
3. 判断控制权限模式（远程/就地）
4. 执行相应的AGC控制逻辑
```

### 2. 判断标准

#### 上调节能力判断
- 计算逆变器基于辐射的功率上限：`功率上限 = 额定功率 × (实际辐射 / 1000W/m²)`
- 如果辐射数据无效（< 50W/m²），则使用额定功率作为上限
- 判断条件：`(功率上限 - 当前功率) > 0.5kW` 则认为能上调
- 如果所有逆变器都不满足上调条件，设置上调节闭锁

#### 下调节能力判断
- 下限功率设为 0kW
- 判断条件：`(当前功率 - 0) > 0.5kW` 则认为能下调
- 如果所有逆变器都不满足下调条件，设置下调节闭锁

### 3. 关键参数

```go
const powerMargin = 0.5  // 功率裕度阈值（kW）
const minPower = 0.0     // 最小功率下限（kW）
```

## 代码实现

### 修改的文件

#### 1. `server/service/agvc/agcv_main/agc.go`

**新增方法：**
```go
func (s *agc) checkAndUpdateRegulationLocks(bwdNo int) error
```

**功能说明：**
- 获取所有在线逆变器列表
- 获取气象站辐射数据
- 遍历每个逆变器，检查其上调和下调能力
- 统计能上调和能下调的逆变器数量
- 根据统计结果设置闭锁状态
- 将闭锁状态写入DataStorage和DispatchStorage

**修改位置：**
- 在 `executeAGCCycle` 方法开始部分调用闭锁检查
- 在 `sendAGCResultToDispatch` 方法中启用闭锁状态发送（取消注释）

#### 2. `server/service/agvc/agcv_main/data_storage.go`

**新增方法：**
```go
func (s *dataStorage) SetData(psid, eqid, eqType, dataType int, point string, value interface{}) error
```

**功能说明：**
- 提供设置数据点值的接口
- 用于更新闭锁状态到内存存储

#### 3. `server/service/agvc/agcv_main/dispatch_storage.go`

**新增方法：**
```go
func (ds *dispatchStorage) SetData(psid, eqid, eqType, dataType int, point string, value interface{}) error
```

**功能说明：**
- 提供设置调度数据点值的接口
- 用于更新闭锁状态到调度存储，供调度系统读取

## 点位定义

根据 `server/service/agvc/cons/def.go` 中的定义：

```go
const (
    AGC_YX_UP_REG_LOCK   = "AGC有功上调节闭锁"  // 点标识: 405
    AGC_YX_DOWN_REG_LOCK = "AGC有功下调节闭锁"  // 点标识: 406
)
```

- **点位405**：AGC有功上调节闭锁（遥信YX）
- **点位406**：AGC有功下调节闭锁（遥信YX）

值定义：
- `0`：未闭锁，允许调节
- `1`：已闭锁，禁止调节

## 调用流程

```
AGC控制循环 (agcControlLoop)
    ↓
执行AGC周期 (executeAGCCycle)
    ↓
检查并更新闭锁 (checkAndUpdateRegulationLocks)
    ↓
    ├─ 获取在线逆变器列表
    ├─ 获取辐射数据
    ├─ 遍历逆变器检查调节能力
    │   ├─ 获取当前功率
    │   ├─ 计算功率上限
    │   ├─ 判断能否上调
    │   └─ 判断能否下调
    ├─ 统计调节能力
    ├─ 设置闭锁状态
    └─ 写入存储
        ├─ DataStorage.SetData()
        └─ DispatchStorage.SetData()
    ↓
继续AGC控制逻辑
    ↓
发送结果到调度 (sendAGCResultToDispatch)
    └─ 包含闭锁状态信息
```

## 日志输出

实现中包含详细的日志记录：

### Debug级别
```
- 逆变器调节能力检查（每个逆变器的详细信息）
- 存在可上调/下调的逆变器，解除闭锁
```

### Info级别
```
- 所有逆变器都无法上调，设置上调节闭锁
- 所有逆变器都无法下调，设置下调节闭锁
- 调节闭锁状态更新完成（汇总信息）
```

### Warn级别
```
- 检查调节闭锁状态失败
```

### Error级别
```
- 更新DataStorage闭锁状态失败
- 更新DispatchStorage闭锁状态失败
```

## 使用场景

1. **光伏电站白天运行**
   - 光照充足时，逆变器功率接近上限 → 设置上调节闭锁
   - 防止AGC系统继续发送上调指令

2. **光伏电站夜间或低辐射**
   - 逆变器功率接近0 → 设置下调节闭锁
   - 防止AGC系统继续发送下调指令

3. **部分逆变器故障**
   - 在线逆变器全部达到上限 → 设置上调节闭锁
   - 在线逆变器全部接近0 → 设置下调节闭锁

## 注意事项

1. **功率裕度阈值**
   - 当前设置为0.5kW
   - 可根据实际需求调整该参数

2. **辐射数据获取**
   - 从气象站（qxyNo=1）获取辐射数据
   - 使用倾斜辐射和水平辐射的较大值
   - 如果辐射数据 < 50W/m²，则使用额定功率作为上限

3. **逆变器筛选**
   - 只统计有效的逆变器（编号和额定功率不为空）
   - 只统计能成功获取当前功率的逆变器
   - 无效逆变器不计入总数

4. **调度系统集成**
   - 闭锁状态会实时发送到调度系统（1189端口）
   - 调度系统可读取闭锁状态来调整控制策略

## 测试建议

1. **模拟高辐射场景**
   - 设置辐射值接近1000W/m²
   - 逆变器功率接近额定功率
   - 验证上调节闭锁是否正确设置

2. **模拟低辐射场景**
   - 设置辐射值接近0
   - 逆变器功率接近0
   - 验证下调节闭锁是否正确设置

3. **模拟正常运行场景**
   - 逆变器功率在10%-90%范围
   - 验证闭锁状态为未闭锁（0）

4. **查看日志输出**
   - 检查调节能力统计是否正确
   - 验证闭锁状态变化日志

## 扩展性

该实现具有良好的扩展性：

1. 可以调整功率裕度阈值
2. 可以为不同品牌逆变器设置不同的判断标准
3. 可以增加更多的闭锁条件（如温度、故障等）
4. 可以实现闭锁状态的历史记录和统计分析

## 相关文档

- AGC控制系统设计文档
- 逆变器通信协议文档
- 调度系统接口规范
