# 华为逆变器常量化改造和模式驱动控制

## 改造概述

本次改造将华为逆变器的点位定义从map结构改为常量结构，并实现了模式驱动的调控流程。

## 主要改动

### 1. 点位常量化

删除了原有的 `HuaweiInverterPoint` 结构体和 `HuaweiInverterPoints` map，替换为：

#### 点位标识常量 (501-515系列)
```go
const (
    // AGC/AVC调节参数
    HW_POINT_REACTIVE_GRADIENT = "501" // 无功功率变化梯度(%/s)
    HW_POINT_ACTIVE_GRADIENT   = "502" // 有功功率变化梯度(%/s)
    HW_POINT_MAINTAIN_TIME     = "503" // 调度指令维持时间(s)

    // 有功调节
    HW_POINT_ACTIVE_KW      = "504" // 有功功率固定值降额(kW)
    HW_POINT_REACTIVE_PF    = "505" // 无功功率补偿(功率因数)
    HW_POINT_REACTIVE_QS    = "506" // 无功功率补偿(Q/S)
    HW_POINT_ACTIVE_PERCENT = "507" // 有功功率百分比降额(0.1%)
    HW_POINT_ACTIVE_W       = "508" // 有功功率固定值降额(W)

    // 调节模式和指令
    HW_POINT_ACTIVE_MODE    = "510" // [有功]调节模式
    HW_POINT_ACTIVE_VALUE   = "511" // [有功]调节值
    HW_POINT_ACTIVE_CMD     = "512" // [有功]调节指令
    HW_POINT_REACTIVE_MODE  = "513" // [无功]调节模式
    HW_POINT_REACTIVE_VALUE = "514" // [无功]调节值
    HW_POINT_REACTIVE_CMD   = "515" // [无功]调节指令
)
```

#### 寄存器地址常量
```go
const (
    HW_REG_REACTIVE_GRADIENT = 42015 // 无功功率变化梯度寄存器
    HW_REG_ACTIVE_GRADIENT   = 42017 // 有功功率变化梯度寄存器
    HW_REG_MAINTAIN_TIME     = 42019 // 调度指令维持时间寄存器
    
    HW_REG_ACTIVE_KW      = 40120 // 有功功率固定值降额(kW)寄存器
    HW_REG_REACTIVE_PF    = 40122 // 无功功率补偿(功率因数)寄存器
    HW_REG_REACTIVE_QS    = 40123 // 无功功率补偿(Q/S)寄存器
    HW_REG_ACTIVE_PERCENT = 40125 // 有功功率百分比降额寄存器
    HW_REG_ACTIVE_W       = 40126 // 有功功率固定值降额(W)寄存器
    
    HW_REG_ACTIVE_MODE    = 35300 // [有功]调节模式寄存器
    HW_REG_ACTIVE_VALUE   = 35301 // [有功]调节值寄存器
    HW_REG_ACTIVE_CMD     = 35303 // [有功]调节指令寄存器
    HW_REG_REACTIVE_MODE  = 35304 // [无功]调节模式寄存器
    HW_REG_REACTIVE_VALUE = 35305 // [无功]调节值寄存器
    HW_REG_REACTIVE_CMD   = 35307 // [无功]调节指令寄存器
)
```

### 2. 模式驱动控制流程

#### 新增枚举类型
```go
// 有功调节模式
type HuaweiControlMode int
const (
    HuaweiModePercentage HuaweiControlMode = 0 // 百分比调节模式
    HuaweiModeAbsolute   HuaweiControlMode = 1 // 绝对值调节模式
)

// 无功调节模式
type HuaweiReactiveMode int
const (
    HuaweiReactiveModePF HuaweiReactiveMode = 1 // 功率因数模式
    HuaweiReactiveModeQS HuaweiReactiveMode = 2 // Q/S模式
)
```

#### 统一调控入口

**AGC调控（有功功率）**
```go
func (h *HuaweiInverterController) ControlAGC(
    inverter *agvc.AgvcNbqSetting, 
    mode HuaweiControlMode, 
    targetValue float64
) error
```

调控流程：
1. 设置有功调节模式 (HW_REG_ACTIVE_MODE)
2. 根据模式选择对应的调节值寄存器并设置值
   - 百分比模式 → HW_REG_ACTIVE_PERCENT
   - 绝对值模式 → HW_REG_ACTIVE_KW
3. 发送有功调节指令 (HW_REG_ACTIVE_CMD)

**AVC调控（无功功率）**
```go
func (h *HuaweiInverterController) ControlAVC(
    inverter *agvc.AgvcNbqSetting, 
    mode HuaweiReactiveMode, 
    targetValue float64
) error
```

调控流程：
1. 设置无功调节模式 (HW_REG_REACTIVE_MODE)
2. 根据模式选择对应的调节值寄存器并设置值
   - 功率因数模式 → HW_REG_REACTIVE_PF
   - Q/S模式 → HW_REG_REACTIVE_QS
3. 发送无功调节指令 (HW_REG_REACTIVE_CMD)

### 3. 新增功能方法

#### SetMaintainTime
设置调度指令维持时间
```go
func (h *HuaweiInverterController) SetMaintainTime(
    inverter *agvc.AgvcNbqSetting, 
    maintainTimeSeconds int
) error
```

### 4. 辅助方法重构

#### GetPointRegister
获取点位对应的寄存器地址，替代原有的 GetPointDefinition
```go
func (h *HuaweiInverterController) GetPointRegister(pointID string) (int, error)
```

#### ValidateControlValue
验证控制值是否在合理范围内，基于常量直接判断
```go
func (h *HuaweiInverterController) ValidateControlValue(pointID string, value float64) error
```

#### ConvertToRegisterValue / ConvertFromRegisterValue
值转换方法，支持所有点位类型的转换
```go
func (h *HuaweiInverterController) ConvertToRegisterValue(pointID string, realValue float64) (int, error)
func (h *HuaweiInverterController) ConvertFromRegisterValue(pointID string, registerValue int) (float64, error)
```

## 兼容性

所有原有的公开方法都得到保留，确保向后兼容：

- `ControlAGCByPercentage(inverter, percentage)`
- `ControlAGCByAbsolute(inverter, powerKW)`
- `ControlAVCByPowerFactor(inverter, pf)`
- `ControlAVCByReactivePower(inverter, qs)`
- `SetControlGradient(inverter, activePowerGradient, reactivePowerGradient)`

## 使用示例

### 新的模式驱动方式（推荐）
```go
// AGC调控
err := HuaweiController.ControlAGC(
    inverter,
    HuaweiModePercentage,  // 百分比模式
    80.0,                 // 目标百分比
)

// AVC调控
err := HuaweiController.ControlAVC(
    inverter,
    HuaweiReactiveModePF,   // 功率因数模式
    0.9,                  // 目标功率因数
)

// 设置维持时间
err := HuaweiController.SetMaintainTime(inverter, 60) // 60秒
```

### 传统方式（仍然支持）
```go
// AGC百分比调控
err := HuaweiController.ControlAGCByPercentage(inverter, 80.0)

// AVC功率因数调控
err := HuaweiController.ControlAVCByPowerFactor(inverter, 0.9)
```

## 优势

1. **性能提升**：常量在编译时确定，避免运行时map查找
2. **内存优化**：移除不必要的结构体，减少内存占用
3. **类型安全**：使用枚举类型替代魔法数字
4. **代码简化**：净减少约100行代码
5. **可维护性**：集中管理点位和寄存器定义
6. **可扩展性**：易于添加新的调控模式

## 修改的文件

1. `server/service/agvc/agcv_main/huawei_inverter_controller.go` - 主要改造文件
2. `server/service/agvc/agcv_main/huawei_point_initializer.go` - 使用新常量

## 支持的指标（14个）

| 点标识 | 常量名 | 描述 | 寄存器 |
|--------|---------|------|--------|
| 501 | HW_POINT_REACTIVE_GRADIENT | 无功功率变化梯度(%/s) | 42015 |
| 502 | HW_POINT_ACTIVE_GRADIENT | 有功功率变化梯度(%/s) | 42017 |
| 503 | HW_POINT_MAINTAIN_TIME | 调度指令维持时间(s) | 42019 |
| 504 | HW_POINT_ACTIVE_KW | 有功功率固定值降额(kW) | 40120 |
| 505 | HW_POINT_REACTIVE_PF | 无功功率补偿(功率因数) | 40122 |
| 506 | HW_POINT_REACTIVE_QS | 无功功率补偿(Q/S) | 40123 |
| 507 | HW_POINT_ACTIVE_PERCENT | 有功功率百分比降额(0.1%) | 40125 |
| 508 | HW_POINT_ACTIVE_W | 有功功率固定值降额(W) | 40126 |
| 510 | HW_POINT_ACTIVE_MODE | [有功]调节模式 | 35300 |
| 511 | HW_POINT_ACTIVE_VALUE | [有功]调节值 | 35301 |
| 512 | HW_POINT_ACTIVE_CMD | [有功]调节指令 | 35303 |
| 513 | HW_POINT_REACTIVE_MODE | [无功]调节模式 | 35304 |
| 514 | HW_POINT_REACTIVE_VALUE | [无功]调节值 | 35305 |
| 515 | HW_POINT_REACTIVE_CMD | [无功]调节指令 | 35307 |

所有14个关键指标都已完成常量化定义和模式驱动控制实现。